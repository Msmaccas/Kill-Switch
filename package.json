{
  "name": "kill-switch",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/core && npm run build --workspace packages/data && npm run build --workspace packages/providers && npm run build --workspace packages/workflows && npm run build --workspace packages/reports && npm run build --workspace packages/server && npm run build --workspace packages/worker",
    "test": "node tests/run-tests.js",
    "smoke": "node packages/worker/dist/smoke.js",
    "start": "node packages/server/dist/index.js",
    "hygiene": "bash scripts/check-repo-hygiene"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.11.19",
    "ts-node": "^10.9.2"
  }
}